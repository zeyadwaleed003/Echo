export enum RevocationReason {
  ROTATION = 'rotation',
  REUSE = 'reuse',
  LOGOUT = 'logout',
  SESSION_LOGOUT = 'session_logout',
  DEACTIVATION = 'deactivation',
  DELETION = 'deletion',
  SECURITY = 'security',
  OTHER = 'other',
}
