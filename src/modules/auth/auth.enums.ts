export enum RevocationReason {
  ROTATION = 'rotation',
  LOGOUT = 'logout',
  SESSION_LOGOUT = 'session_logout',
  DEACTIVATION = 'deactivation',
  DELETION = 'deletion',
  SECURITY = 'security',
  OTHER = 'other',
}
