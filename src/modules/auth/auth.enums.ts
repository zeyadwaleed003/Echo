export enum RevocationReason {
  ROTATION = 'rotation',
  REUSE = 'reuse',
  LOGOUT = 'logout',
  PASSWORD_CHANGE = 'password_change',
  DEACTIVATION = 'deactivation',
  DELETION = 'deletion',
  SECURITY = 'security',
  OTHER = 'other',
}
